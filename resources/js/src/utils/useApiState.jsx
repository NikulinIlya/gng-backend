import React, { useState, useEffect, useReducer } from 'react';

export default function useApiState(){
    const {  } = useReducer(apiStateReducer,{})
}

function apiStateReducer(state){
    // switch 
}